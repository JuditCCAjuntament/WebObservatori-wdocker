apiVersion: v1
kind: Service
metadata:
  name: ciutat-agora-api-stage
  labels:
    app: ciutat-agora-api-stage
  namespace: stage
spec:
  selector:
    app: ciutat-agora-api-stage
  type: ClusterIP
  ports:
    - name: http
      port: 80
