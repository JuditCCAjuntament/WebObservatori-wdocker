apiVersion: apps/v1
kind: Deployment
metadata:
  name: ciutat-agora-api-stage
  labels:
    app: ciutat-agora-api-stage
  namespace: stage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ciutat-agora-api-stage
  template:
    metadata:
      name: ciutat-agora-api-stage
      labels:
        app: ciutat-agora-api-stage
    spec:
      containers:
      - name: ciutat-agora-api-stage
        image: eu.gcr.io/websmunicipals/ciutat-agora-api:devel-latest
        imagePullPolicy: Always
        ports:
          - containerPort: 80
        envFrom:
          - configMapRef:
              name: usuaris-api-configmap
          - configMapRef:
              name: media-api-configmap
          - secretRef:
              name: mysql-ciutat-agora-credentials
          - configMapRef:
              name: redis-4-server-configmap
          - configMapRef:
              name: mysql-57-server-configmap
        resources:
          requests:
            memory: 100Mi
            cpu: 10m
          limits:
            memory: 256Mi
            cpu: 100m
