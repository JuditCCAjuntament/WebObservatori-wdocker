apiVersion: v1
kind: Service
metadata:
  name: ciutat-agora-web-stage
  labels:
    app: ciutat-agora-web-stage
  namespace: stage
spec:
  selector:
    app: ciutat-agora-web-stage
  type: ClusterIP
  ports:
    - name: http
      port: 80
